# Foyer 项目交接文档

> 生成于 2026-09-19。用途：迁移到新对话时作为完整上下文。
> 仓库 `github.com/TobyMint/foyer`（私有），本地 `/root/paper/foyer`。
> **已推送状态：HEAD = `819c620`，与 origin/main 同步（0 未推送）。**

---

# 0. 一页速览

**项目**：Foyer —— 面向 **agentic LLM 会话服务**的**准入控制**，目标是**内存受限单卡**（RTX 3090 24GB）场景。

**一句话主张**：不做"同时放几个会话"的静态限额（cap-n），而是**按容量核算准入**——用每个会话的历史增长预测它最终要占多少 KV，加总不超过池子预算就放行。**因此换场景不用重新扫参。**

**投稿目标**：CCF-B / 中科院二区，时间窗 **2026 年 10–11 月**（约剩 6 周）。

**当前站位（诚实版）**：
- ✅ 系统完整可用，45+ 个有效 run，全表可一键重建
- ✅ 有一个**扎实的机制性发现**（控制面对参数极度敏感；驱逐存在内部最优点）
- ⚠️ **在 200 齐射档，我们的吞吐仍打不过调优过的静态 cap2**（311.4min/64.8% vs 301.7min/67.0%）
- ⚠️ 但**在另外三种到达形状下我们领先**（25 齐射、Poisson 200、真实高峰）
- ⚠️ 主对比对象 Concur 的复现刚刚修完三处缺陷，5 个 run 只跑完 2 个

**当前阻塞**：lab 卡被邻居占用 + Concur 论文原参数在我们的设置上**死锁**（原因未完全查清）。

---

# 1. 研究问题与三个机制

## 1.1 问题

一个 coding agent 干活是**一轮一轮**的：每轮要把之前所有内容重新喂给模型。为了不重算，引擎缓存已算过的前缀——即 **KV 缓存**。上下文随轮次变长。

**我们的实测规模**：

```
KV 池总容量          101,432 token
一个会话的中位占用    23,187 token  = 池子的 22.9%
               p75   34,477 token  = 34.0%
               p90   51,064 token  = 50.3%
               最大   89,436 token  = 88.2%
```

**一个会话就能吃掉池子的 23–88%。** 这是整个项目最关键的结构性事实——它使所有"细粒度利用率"假设失效。

无准入控制的后果（实测，200 档）：

```
命中率 0.0%   TTFT 中位 2,415,006 ms（40 分钟）   失败 121 / 999 步
```

## 1.2 三个机制

**机制①　容量核算准入**（核心贡献）

准入判据：

```
m × [ Σ_{已准入会话}( x_i + ĝ_i ) + ( x_新 + ĝ_新 ) ]  ≤  ρ × C_pool
```

- `x` = 会话当前上下文（在场者实测，新到者取首轮 prompt）
- `ĝ` = 未来 H=3 轮的增长估计（会话内 EMA 混合全局先验）
- `m` = 会话级安全余量（1.15）
- `ρ` = 池子预算系数（0.75 默认 / 0.95 修复配置）
- 配套 **named permit**：控制器按会话 ID 发许可，保证"计划=执行"，顺带实现会话级 pause/resume

**机制②　状态保留式挂起**（使能器）

用 SGLang 原生 HiCache 开 host 梯队：挂起的会话状态留在主机内存，复活时不必重算。

**机制③　有界复活（W/T ≥ β）**——**仅设计，未实现、未验证**，论文里必须明确标注。

---

# 2. 实验设置

| | |
|---|---|
| 硬件 | RTX 3090 24GB 单卡（lab-3090 = `100.64.0.3`）|
| 推理栈 | SGLang 0.5.10 |
| 模型 | Qwen2.5-Coder-7B-Instruct，YaRN ×3（96K 上下文）|
| KV 池 | **101,432 token**（`mem-fraction-static 0.85`，全档位统一）|
| 负载 | 真实 coding-agent trace，多档（25 / 200 会话），会话内闭环重放 |
| 指标 | 前缀命中率、TTFT p50/p90、SLO(<10s)%、makespan、时间平均并发、**压力驱逐量** |

**指标口径**：`server_prefix_hit_rate` = 服务端实测 cached_prompt / prompt（不是 trace 的计划值）。

---

# 3. 核心数据（全部有效结果）

## 3.1 静态 cap 悬崖（25 档）

| cap | wall(min) | 命中% | TTFT p50(ms) | SLO% | 并发 |
|---|---|---|---|---|---|
| 1 | 63.3 | 68.2 | 1273 | 95.2 | 1.00 |
| 2 | 37.4 | 68.1 | 1369 | 93.6 | 1.98 |
| 3 | 30.4 | 64.9 | 1655 | 86.4 | 2.73 |
| 4 | 30.4 | 55.2 | 2528 | 74.4 | 3.49 |
| 5 | 28.7 | 36.6 | 11028 | 46.4 | 4.80 |
| 6 | 27.9 | 31.7 | 13505 | 36.0 | 5.12 |
| 7 | 32.1 | 7.5 | 38146 | 6.4 | 6.42 |
| 8 | 33.4 | 4.5 | 57107 | 3.2 | 7.70 |

**甜点区极窄**：cap3→cap4 掉 9.7pp，cap5 腰斩，cap7 归零。

## 3.2 200 档主表

| 策略 | wall(min) | 命中% | TTFT p50 | SLO% | 失败 |
|---|---|---|---|---|---|
| default（无控制）| 228.1 | 0.0 | 2415006 | 0.2 | **121** |
| cap1 | 515.7 | 69.9 | 1595 | 94.8 | 0 |
| **cap2** | **301.7** | **67.0** | 2117 | 90.0 | 0 |
| cap3 | 249.4 | 59.0 | 3466 | 76.9 | 0 |
| cap4 | 265.6 | 45.1 | 8401 | 52.8 | 0 |
| cap5 | 265.5 | 28.0 | 22732 | 31.0 | 0 |
| Foyer（旧配置）| 459.1 | 69.8 | 1902 | 92.6 | 0 |
| **Foyer（去节流后）** | **311.4** | **64.8** | 2442 | 87.3 | 0 |

**帕累托判定**：cap2 **双轴严格占优**当前 Foyer。旧配置未被占优（高 2.8pp 但慢 52%）。

## 3.3 到达形状（三种）

**Poisson λ=0.04/s，200 会话**

| 策略 | wall | 命中% | SLO% |
|---|---|---|---|
| cap2 | 310.5 | 67.7 | 89.5 |
| cap3 | 259.2 | 49.8 | 62.2 |
| cap4 | 257.6 | 45.9 | 55.7 |
| Foyer 修复配置 | 299.3 | 65.6 | 88.6 |

**真实高峰小时（117 会话，实测到达时间）**

| 策略 | wall | 命中% | SLO% |
|---|---|---|---|
| cap2 | 131.9 | 64.5 | 100.0 |
| Foyer 修复配置 | **79.3** | 59.8 | **100.0** |

**Foyer 快 40%，SLO 并列 100%。**

## 3.4 节流消融（25 档）

| 配置 | wall | 命中% | 并发 |
|---|---|---|---|
| baseline `budget3` | 62.1 | 67.7 | 1.03 |
| 关单飞 `sf=0` | 53.2 | 66.4 | 1.25 |
| **关高水位地板 `hw=0`** | 41.8 | 65.0 | 1.87 |
| `target=0.95` | 43.4 | 62.3 | 1.72 |
| **`hw=0;target=0.95`** | **33.0** | 64.4 | 2.28 |
| （重复）| 33.9 | 64.4 | 2.42 |

**主节流是两个 v0.5 遗留安全机制**（高水位地板 + 单飞准入），不是容量核算本身。**旧配置 35% 的墙钟时间 GPU 在空转。**

## 3.5 HiCache（25 档 cap6）

| | wall | 命中% | 压力驱逐 |
|---|---|---|---|
| cap6 无 HiCache | 27.9 | 31.7 | 1,178K |
| **cap6 + HiCache** | **22.8** | **64.1** | 1,320K |

**同一 cap，开 HiCache 后又快又好**——驱逐代价从"重算"变成"主机内存取回"。

## 3.6 驱逐曲线（45 个 run，关键新发现）

**先扣除地板**：每个 run 都驱逐约 **981,942** token 的会话正常结束清理（cap1 并发 1 也驱逐 981,957）。剩下的才是压力驱逐。

```
压力驱逐(K/会话)  0      4      40     47     79     83
wall(min)       63.3 ─ 30.4 ─ 28.7 ─ 27.9 ─ 32.1 ─ 33.4
                              ↑ 内部最优
命中率(%)       68.2 ─ 64.9 ─ 36.6 ─ 31.7 ─ 7.5 ─ 4.5
```

**代价曲线是 U 形的，不是单调的。** 零压力驱逐（cap1）是全表最慢策略；驱逐过量也变慢（重算主导）。

## 3.7 预测器归因（25 档）

| 臂 | wall | 命中% |
|---|---|---|
| 零增长 `predictor=zero` | 46.7 | 65.7 |
| 全局均值 `global` | 58.9 | 66.8 |
| EMA（默认）| 54.8 | 66.8 |
| oracle（开天眼）| 92.4 | 68.2 |
| EMA 关饥饿保护 `sg=0` | 76.7 | 68.2 |

**⚠️ oracle 臂的实现有缺陷**（见 §7）：旧版把会话全生命周期 input+output 累加后恒定返回，既不衰减也不受 3 轮窗口约束，还重复计入首轮 input。**已重写为 `G*(t) = max(0, C(t+3) − C(t))`，重跑尚未做。**
**论文里"越准的预测越保守"这句必须删除**——它建立在这个有缺陷的实现上。

**诚实读数**：EMA 比零增长多花 17.4% 时间，只多 1 个请求跨过 SLO 线。**预测器的独立收益尚未证明**，只应表述为"尚未证明"，不能写"没必要"。

## 3.8 Concur 重跑（进行中，5 个 arm 完成 2 个）

Concur 论文参数（§5.1）：`U_low=0.2, U_high=0.5, H_thresh=0.2, α=2, β=0.5`。
附录 A.1 自测：`U_high=0.8` 退化 4–5×，`U_low≥0.3` 退化 2–3×。

| arm | u_low | u_high | wall(min) | 命中% | TTFT p50 | 状态 |
|---|---|---|---|---|---|---|
| `aimd2_ul35` | 0.35 | 0.5 | 287.5 | 45.17 | 6205 | ✅ |
| `aimd2_uh75` | 0.2 | 0.75 | **283.8** | **57.98** | 3633 | ✅ |
| `aimd2_paper` | 0.2 | **0.5** | — | — | — | ❌ **死锁** |
| `aimd2_uh06` | 0.2 | 0.6 | — | — | — | ⏸ |
| `aimd2_paper_rawH` | 0.2 | 0.5（旧 H 门）| — | — | — | ⏸ |

**重点观察**：论文原参数死锁；单独抬高任一阈值都能跑通；且 `u_high=0.75` 的质量**优于** `0.5`，**与论文"0.5 最优"的结论相反**。

**⚠️ 但这不是干净的对比**：两个完成的 arm 各自同时只变了一个参数是运气，每个 arm **n=1**，且缺 `(0.35, 0.75)` 才能拆开双因子。**不能下结论。**

---

# 4. 关键发现（按重要性排序）

**K1　控制面对参数极度敏感，甜点区极窄。**
两条独立证据：我们的 cap 悬崖（cap3→cap5 命中腰斩）+ Concur 自己附录 A.1 的敏感性表（U_high 0.8 退化 4–5×）。**这是机制①存在的最强理由**：静态阈值需要"控制面够宽"这个前提，边缘场景不满足。

**K2　驱逐存在内部最优点，不是越少越好。**
代价曲线 U 形，零驱逐（cap1）最慢。**用 U 形曲线而非"越少越好"表述。**

**K3　准入突发性是真实的效率损失。**
我们 30 秒窗口内准入 ≥2 个的占比 **38%**，cap2 只有 **18%**；我们并发 max=4 vs cap2 max=2；均值几乎一样（1.33 vs 1.28）。结果：**驱逐更多（48.3K vs 45.2K/会话）却命中更低（64.8% vs 67.0%）**——纯亏。
**假设**：同一窗口进来的会话初始都很小（账面上塞得下），随后**同步增长**造成瞬时超额。即**判据把增长当成独立的，实际是相关的**。此假设**有相关性证据，无因果证明**。

**K4　HiCache 改变驱逐的代价，而我们的控制器不知道它开着。**
cap6 vs cap6+HiCache 是直接证据。**推论**：ρ 不该是固定常数，应由实测驱逐代价导出。**这是把"旋钮"变成"测量"的路径，属于方法学贡献。**

**K5　Concur 的阈值在边缘场景不可迁移（待证实）。**
结构性理由：Concur 的 U 是 KV 利用率，其 `[0.2, 0.5]` 死区设计前提是利用率是细粒度连续信号。我们的池子里**一个会话就占 23–88%**，利用率退化成台阶，**死区比一个会话还窄**。§3.8 的实测与该推理一致，但**样本不足以下结论**。

---

# 5. 数据资产与作废清单

## 5.1 有效资产

- **45 个 25 档 run + 30+ 个 200 档 run**，全部池子对齐 101,432
- 三档到达形状（齐射 / Poisson / 真实高峰）
- 节流消融、HiCache 对照、预测器归因、驱逐数据
- **`docs/tables.md` 可由 `make_tables.py` 一键重建**
- **`docs/runs_registry.md`**：81 个 run 的登记表，异常池子标 ⚠

## 5.2 已作废（勿引用）

| 类别 | 原因 |
|---|---|
| 7 个 run | **池子污染**（48,234 / 84,528 / 97,367），已移入 `results/night/_invalid/` |
| 全部旧 aimd2 结论 | 阈值偏离论文 + H 门失效（§7）|
| oracle 臂的解释 | 实现有缺陷（§3.7）|
| `cap4_r2/r3` | 池子 114,495 且 trace 版本不同，**不能**与 `budget200c_cap4` 组成重复实验 |
| 早期 25/50 档 | 三种池子混用（114,495 / 72,647 / 101,432）|

---

# 6. 基础设施

## 6.1 机器

- **lab-3090**（`100.64.0.3`）：4× RTX 3090 24GB，**我们的主战场**
- **lab-A100**（`100.64.0.2`）：3× A100 80GB。**不可用作退路**——利用率常驻 60–91%（wall time 会被污染），且无 turnstile 环境 / 模型 / trace

## 6.2 目录

```
/data/xbw/turnstile/
  scripts/                       ← 一份脚本副本
  TraceLab/replay/scripts/       ← ⚠️ 另一份，lane 实际调用的是这份
  TraceLab/replay/target/release/session_runner   ← Rust runner
  models/qwen25-coder-7b-yarn96k
  data/replay_night200_clean.csv  ← 清洁 trace（200 档）
  results/night/<run>/           ← 每个 run 一个目录
```

**⚠️ 双脚本树是已踩过的坑，见 §7。**

## 6.3 每个 run 目录的内容

```
summary.json        指标汇总（runner 写）
metadata.json       溯源（run_matrix 写）
steps.jsonl         每步记录（prompt_len / TTFT / 命中 等）
admissions.jsonl    准入事件（queued/admit/release/pause/resume）
controller.jsonl    控制器决策
metrics.csv         引擎指标时间序列（含 evicted_tokens_total）
runner.out          进度
```

## 6.4 管线

```
queue_driver2.sh          每卡一条 lane，等空闲→跑→重试，直到 summary 落地
  └ wait_gpu_then_run.sh  等 GPU 空闲（当前阈值 FREE_MIB=1000）
      └ lane_*.sh         一条 lane 跑若干 arm（set -e）
          └ run_matrix.py 起 server → 校验池子 → 起 controller → 跑 runner
              └ controller_*.py
              └ session_runner（Rust）
      └ lane_done.sh      仅当所有 arm 都有 summary 才标记完成
```

**关键机制**：
- **池子守卫**：起服后校验池子，不符则按实测斜率解算 memfrac 重试（上限 0.90），仍不符则中止且不记录
- **按 run 跳过**：已有 summary 的 run 会被跳过，整条 lane 重试只重做未完成的
- **metadata 提前落盘**：配置确定即写，被杀只损失结果不损失溯源

---

# 7. 已知缺陷与技术债

## 7.1 已修（本次会话）

| # | 缺陷 | 影响 |
|---|---|---|
| 1 | **lab 双脚本树**：lane 调用 `TraceLab/replay/scripts/`，而修复只同步到 `scripts/` | 5 个 run 会因 `--h-mode` 未识别而 argparse 报错**全灭** |
| 2 | **H 门在无测量时反而触发 cut**（`not hit_measured or ...`）| 与注释和向审稿人报告**相反**；已改为 `hit_measured and hit < h_thresh` |
| 3 | **oracle 消融目标错误**：返回生命周期总和且恒定不衰减 | 论文"越准越保守"结论不成立 |
| 4 | **route_mode 空洞**：`budget3_*` → `budget3_ablation` 无分支处理 → 空 cap_args → **无门控 runner** | 已加 `HANDLED_ROUTES` 守卫 |
| 5 | **manifest 缺失**：无代码版本 / argv / 环境变量 | 已改内容哈希 + 真实 argv + path_divergence |
| 6 | GPU 空闲阈值过松（300 MiB 放过 254 MiB 残留）| 已改为解算器补偿 + FREE_MIB=1000 |
| 7 | driver 按日志行等 wave 1（那行早写过）→ 永远等待 | 已改为按进程判断 |
| 8 | driver 的 work-list 标记用 lane 的**第一个** run | 首个 run 完成后整条 lane 被跳过，剩余 arm **永不运行** |
| 9 | metadata 只在结束后写 | 被杀则完整结果丢失溯源 |

## 7.2 未修（技术债）

| # | 缺陷 | 状态 |
|---|---|---|
| A | **`controller_aimd2.py` 没有 `admit` 事件处理器** | 已确认。被准入会话状态停在 `waiting` |
| B | **控制器状态与日志不符**（未解释）| 430 条决策中 `waiting_n` 峰值仅 16，而日志有 200 个 queued；用正确状态机重放同一文件得 181，与实报 0 矛盾。**未查清** |
| C | **Concur 论文参数死锁** | u_low=0.2 + u_high=0.5 组合下窗口钉在 1、引擎全空闲、永不发 permit |
| D | **runner 无断点续跑** | 被杀即整轮重来（`aimd2_paper` 曾损失 4.7 小时）|
| E | `ctx` 只更新到已完成轮 | 滞后一轮，量级约池子的 2%（中位每轮增长 2,068 token）。**方向是偏向超发** |
| F | metadata 的 `controller_params` 在提前落盘之后才设 | 早期 metadata 缺该字段 |
| G | ρ×m 因子网格未做 | 见 §10 |

---

# 8. 外部审计史（与 GPT 的四轮交锋）

GPT 审的是 **`bbc2283`**（当时落后 HEAD 11 个 commit，因为未推送）。**已于本次会话推送，后续审计应基于 `819c620`。**

**第一轮（GPT → 我）**：提 4 条，**我核查后全部属实**——oracle 实现问题、EMA 收益未证明、route_mode 静默不限流、主表比较条件（含 cap4 混池）。

**第二轮（我 → GPT，GPT 纠正我 3 处，全对）**：
- 我提的 oracle 修法"剩余 − 已发生"**重复扣减**；GPT 的 `G*(t)=max(0,C(t+3)−C(t))` 才对
- **我拿错了 cap4 的对照行**：GPT 原比的是 200 对 200，我换成 `load25c_cap4` 才出现跨档，然后把自己引入的错配说成"更严重"
- 别急着宣布预测器无价值；"突发→驱逐多→命中低"不能单独推出"纯亏"

**第三轮（GPT → 我，我接受）**：
- "两个阈值行为相同" ≠ "H 门无效"——H≈0.94 时门**确实在拦**，是**饱和**不是**消失**
- 不同 commit 可以放同一张表，关键是**相关实现是否可比**；这条让我发现**我过度自责了**（我的 run_matrix 改动全在 aimd2 路径内，`controller_budget3.py` 一字未改）

**第四轮**：GPT 认可并给出合并执行序（先推送 → ctx 与 oracle 一起验收 → 路由测试 + manifest → Concur 冒烟 → 冻结）。

**教训（GPT 原话，已采纳为工作准则）**：
> **下次最有价值的交付是可核验的提交、测试输出和明确版本的实验结果。**

**冒烟测试确实抓到了两个真 bug**（H 门方向反了 + 双脚本树）——GPT 坚持要它是对的。

---

# 9. 战略决策记录

**D1　Concur 降级为主表一行，不做主对比。**
理由：Concur 是 **offline batch** 场景，论文中 **TTFT / p95 / p99 / percentile / Poisson / arrival 出现次数均为 0**，"SLO" 的 11 次命中全是 `slot`/`slots`/`slowdowns` 的子串。**拿 SLO 去"赢"它，在审稿人眼里无效。**
替代用法：**用 Concur 自己附录 A.1 的敏感性表 + 我们的悬崖**作为两条独立证据，论证"控制面窄 → 需要容量核算而非阈值"。**不攻击 Concur，不冒政治上得罪审稿人圈子的风险。**

**D2　叙事三段论**（用户提出，我修正一处）：
1. **不控制 → 会崩**（0% 命中、40min TTFT、12% 失败）
2. **cap-n → 有效，但"值"是死的**（好值只能靠全量扫描；甜点区极窄；一个 n 定死后对池子/负载/目标都不适应）
3. **我们的方案 → 免调参**

⚠️ **绝不可写"最好的 n 也不如我们"**——我们自己的数据（cap2 301.7 vs 我们 311.4）会当场推翻。正确表述：**"同质量下我们最快、且不需要调参"**，并如实报告吞吐角的差距。

**D3　术语**：停用"柱一/柱二/柱三"，用**创新点 1/2/3** 或报告的 3.1/3.2/3.3 编号。

**D4　引用红线**：arXiv 2608.30830 自报对 MORI/Continuum 的 head-to-head 胜利已否证（0-2），**禁引**；其 41% goodput 损失与 ~10× 复活差可引（2-0）。PrefixShield 2608.01657 已占"admission responsibility"术语，需避撞。

**D5　n 的期待管理**：cap-n 的优势不是"被我们碾压"，是**占据帕累托前沿的不同点**。我们的立论是 **n 不可先验知道 + 需要全量扫描**，不是"我们更快"。

---

# 10. 待办与优先级

| 优先 | 事项 | 需要 GPU | 备注 |
|---|---|---|---|
| **P0** | **删掉草稿 §5.3 的"越准的预测越保守"** | ❌ | **眼下唯一还挂在论文里的错误结论** |
| **P0** | **写离线 tick 重放测试**，定位缺陷 B（181 个会话消失）| ❌ | 未查清前**不改控制器** |
| **P0** | 修缺陷 A（`admit` 处理器缺失）| ❌ | 需先由 B 的结论决定修法 |
| **P1** | 把剩余 3 个 Concur run 重排到两张卡 | ❌ | gpu3 lane 已排空，3 个 run 全压单卡 |
| **P1** | 补跑 `(0.35, 0.75)` 拆开双因子 | ✅ | 才能立住 K5 |
| **P1** | 重画驱逐曲线图（按档位分面）| ❌ | 用户反馈现版"不够清楚" |
| **P2** | ρ×m 因子网格（25 档，6 run）| ✅ | **已撤回优先**：若 K3 成立，放宽预算会放大同步增长超额，应先修准入决策 |
| **P2** | pool/workload 漂移实验 | ✅ | 证明 cap-n 的 n 会失效——**这是我们真正的立论** |
| **P2** | PCIe 重载 vs 重算微基准 | ✅ | 机制③的 β 参数 |
| **P3** | 实现机制③ 有界复活 | — | 目前仅设计 |
| **P3** | HiCache 200 档开关对照 | ✅ | 机制②的关键证据 |
| **P3** | runner 断点续跑 | — | 抗外部击杀 |

---

# 11. 关键文件索引

```
paper/draft-v1.zh.md          论文中文草稿（v1.1）★ 注意 §5.3 的错误结论
docs/tables.md                全部表格（make_tables.py 一键重建）★
docs/runs_registry.md         81 个 run 的登记表
paper/figs/                   6 张图
  fig_eviction_curve.png      ★ 最新，驱逐-质量-代价曲线
  fig_cap_cliff_25tier.png    悬崖
  fig_pareto_200.png          200 档帕累托
  fig_admission_accounting.png 机制①示意
  fig_host_tier.png           机制②示意
  fig_bounded_revival.png     机制③示意
scripts/make_tables.py        表格重建
scripts/run_matrix.py         ★ 生产驱动（池子守卫 + 解算器 + manifest）
scripts/controller_budget3.py ★ Foyer 控制器（含 oracle 修复）
scripts/controller_aimd2.py   Concur 复现（⚠️ 缺陷 A）
scripts/smoke_aimd2_hsignal.py H 信号冒烟测试（无需 GPU，6 项）
tests/test_oracle_forecast.py oracle 单测（8 项）
tests/test_run_matrix_routing.py 路由单测（2 项）
```

---

# 12. 协作约定

**用户偏好**
- **发消息时不要用 LaTeX**——用户在 TUI 里看，公式渲染不出来。用纯文本缩进块 + `rho`/`alpha` 拼写或中文名。论文正文该用照用
- 用户会把产出交给 GPT 审再拿回来：**结论表述要保持可核查的证据边界，避免过度自信措辞**（已被抓过三次）
- 用户写自己的周报，我提供素材
- 不再使用的数据**直接丢掉**，不要留

**工作方式**
- 用户明确要求**自主推进**，不要每步都问
- **凡发车必验启动**（曾因脚本名不匹配空转一夜）
- **改代码前必须确认改的是 lane 实际引用的路径**——不能假定（双脚本树的教训）
- **冻结纪律**：正式重跑前冻结版本；重跑期间改共享 runner / 指标逻辑必须另立版本，不能最后统一贴"冻结版"标签
- **外部击杀是常态**（已 6 次）：卡被占满时我们的长任务会被杀

**当前 lab 环境的风险**
- 4 卡多用户、无调度器、先到先得
- 邻居的 CUDA 上下文会**静默改变我们的池子**（254 MiB → 池子少 4,034 token，约 16 token/MiB）
- **看 `nvidia-smi` 剩余显存会误判**：邻居占 4.3 GB 时表面还剩 20 GB，实际池子只有约 33,000（需 101,432）
