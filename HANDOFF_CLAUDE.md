# Foyer 项目交接（迁移到 Claude Code 用）

> 生成于 2026-09-20 21:50。**这是临时迁移文档，迁移完成后可删。**
> 用途：把当前全部上下文交接给接手的新 agent；如需长期状态，见 `docs/STATE.md`。
>
> 建议阅读顺序：**本文件 → `docs/STATE.md`（工程状态/决策/缺陷）→ `paper/draft-v1.zh.md`（论文草稿）
> → `docs/tables.md`（数据表，可一键重建）→ `REVIEW.md`（对外审计口径）→ `docs/parked_scenarios.md`（封存数据）**

---

## 0. 一页速览

- **项目**：Foyer —— 面向显存受限单卡（RTX 3090 24GB）的 **agentic LLM serving 准入控制**。
- **三个创新点（统一口径，不要再拆成 4/5 个）**：
  1. **容量核算准入（含命名许可）**：按 KV token 记账放行，控制器写下的是确切的会话 ID 集合（计划即执行）；
  2. **状态保留式挂起**：被暂停会话的 KV 下沉主机内存（HiCache），复活是搬运不是重算；
  3. **有界复活**：按"等待时间 ÷ 复活代价"排序回归 —— **仅设计，未实现、未验证**。
- **论文目标**：CCF-B / 中科院二区，时间窗 **2026 年 10–11 月**。
- **当前主场景**：**泊松到达**（`replay_pois200_l04.csv`，200 会话，λ=0.04/s，按语料实测峰值小时率标定）。
  **齐射与真实到达已封存**（数据有效、不再新增 run，见 `docs/parked_scenarios.md`）。
- **当前状态（21:50）**：泊松主表 12 项中 7 项已落地、2 项在跑、3 项排队；
  **最关键的判决 = `t90`（ρ=0.90）能否四项全胜静态 cap2**，预计明早 07:30 前后。

---

## 1. 问题与机制（30 秒版）

Agentic 会话上下文长且只增不减，24GB 卡上 7B 模型的 KV 池只有约 10 万 token，**一个会话就能占 23–88%**。
准入判据（代码名 `budget3` = 论文里的 Foyer）：

```
m × [ Σ_{已准入}(x_i + ĝ_i) + (x_新 + ĝ_新) ] ≤ ρ × C_pool
```

- `x` 实测上下文；`ĝ` 未来 3 轮增长估计（会话内 EMA + 全局先验）；`m`=1.15；`ρ`=target（当前 0.95）。
- **命名许可**：控制器写确切会话 ID 集合，保证"计划放行的 = 实际运行的"，并支持轮次边界 pause/resume。
- 保留的安全阀：瞬时占用硬闸、TTFT-SLO 熔断（唯一反应式成分，10s）、限频饥饿保护。
- 已回收：**高水位地板**（实测虚占 29% 预算）。**单飞准入保留**（地板修复后不再是瓶颈：关掉它只换 +1.2pp 命中、慢 3%）。

---

## 2. 数据现状（泊松主表）

统一 KV 池 **101,432 token**，每个 run 起服时校验池子，不符即中止重试。墙钟取 `metadata.wall_s`。

| 策略 | 墙钟(min) | 命中% | SLO@10s% | TTFT p50 | 状态 |
|---|---|---|---|---|---|
| 不限流 default | 294.8 | 1.6 | 3.0 | 1774s | ✅ |
| 静态 cap1 | — | — | — | — | ⏳ 跑着（219/999）|
| **静态 cap2** | **310.5** | **67.7** | **89.5** | **2.18s** | ✅ 目前最强静态 |
| 静态 cap3 | 259.2 | 49.8 | 62.2 | 5.09s | ✅ |
| 静态 cap4 | 257.6 | 45.9 | 55.7 | 7.21s | ✅ |
| 静态 cap5 | 267.0 | 27.5 | 27.1 | 24.41s | ✅ |
| **Foyer** | **299.3** | **65.6** | **88.6** | **2.37s** | ✅ 主表行 |
| Foyer + HiCache | — | — | — | — | ⏳ 跑着（869/999）|
| Concur（重调参数 0.35/0.75）| 264.6 | 40.1 | 52.8 | 8.55s | ✅ |
| Concur（原论文参数 0.2/0.5）| — | — | — | — | … 排队 |
| Foyer ρ=0.90 | — | — | — | — | … 排队（关键）|
| Foyer ρ=0.85 | — | — | — | — | … 排队（备选）|

**读者须知**：
- SLO 定义 = TTFT<10s 的**步**占比（分母是成功步）；TTFT 从"请求提交给引擎"算起，**不含门外排队与暂停**（受控策略同口径）。
- 10s 是**我们选定的阈值**，不是外部标准；敏感性已验证：阈值取 1s–60s 时排序完全不变（见 `paper/figs/fig_ttft_cdf.png`）。
- 图：`paper/figs/fig_poisson_status.png`（状态总览）、`fig_ttft_cdf.png`（CDF，阈值无关视角）；两者都会同步到 `C:\Users\X2406\Downloads\Foyer_images\`。

---

## 3. 正在推进的队列（自愈机制）

**只用两张卡**（gpu2 / gpu3，lab 是共享机器，用户明确要求别占更多）。

| 卡 | 正在跑 | 之后自动接 |
|---|---|---|
| gpu2 | `foyer_hc`（HiCache，~87%）| `aimd2_paper`（Concur 原参数）→ `t85`（ρ=0.85）|
| gpu3 | `cap1`（~22%，慢档）| → 迁移 → `t90`（ρ=0.90）|

**预计**：`foyer_hc` 今晚 ~22:15；`cap1` 凌晨 ~02:00；`aimd2_paper` 凌晨 ~03:30–05:00；
**`t90` 明早 ~07:30（关键判决）**；`t85` 明早 ~08:30–10:30。

### 队列机制（务必理解，否则会误判"任务停了"）

```
cron(每5分钟) watchdog.sh
  └─ 若某张卡的工作流进程不在 → setsid 拉起 queue_stream.sh <gpu> <worklist>
       └─ 逐行读 worklist（每行 = <lane 脚本> <标记 run>）
            ├─ 标记 run 的 summary.json 已存在 → 跳过
            └─ 否则 → wait_gpu_then_run_fast.sh（30s 轮询，2 次空卡即起跑）→ lane 脚本
                 └─ run_matrix.py：起服 → 校验池子 → 起 controller/monitor → 跑 runner
            └─ lane 失败（set -e）→ 等 10 分钟重试整条 lane；run_matrix 会跳过已有 summary 的 run
```

- **完成信号 = `<标记 run>/summary.json` 存在**；标记必须是 lane 的**最后一个** run（历史教训：用第一个 run 当标记会让整条 lane 被跳过）。
- `migrate_gpu3.sh`（每 5 分钟，一次性）：gpu3 的工作流内存里还缓存着旧清单，等 `pois200_cap1` 落地就杀掉它，让看门狗用新清单重启（否则会去跑已取消的齐射任务）。
- `status_snapshot.sh`（lab 每小时）：写 `results/night/status_snapshots.log`，独立于任何 agent 的运行记录。
- **WSL 侧的会话定时唤醒已撤销**（2026-09-20，迁移到 Claude Code 前移除；crontab 现在为空）。
  原实现是 `scripts/hourly_check.sh` + `codex queue` 注入巡检提示词，已随迁移删除。
  **lab 侧的小时快照不受影响**，仍在 `results/night/status_snapshots.log` 独立记录队列状态。
  若要在 Claude Code 里复刻"定时唤醒"，原理相同（系统 cron + 该 CLI 的消息注入原语），但原语名称需在那边确认。

---

## 4. 战略决策记录（D1–D8 + 本轮新增）

| # | 决策 | 要点 |
|---|---|---|
| D1 | Concur 降级为主表一行 | 它是 offline batch，论文里 TTFT/p95/Poisson 出现次数为 0；不拿 SLO"赢"它，用其附录 A.1 敏感性 + 我们的悬崖作证据 |
| D2 | 叙事三段论 | 不控制→崩；cap-n→有效但值是死的；我们→免调参。**绝不可写"最好的 n 也不如我们"** |
| D3 | 术语 | 用"创新点 1/2/3"，停用"柱一/柱二/柱三" |
| D4 | 引用红线 | arXiv 2608.30830 禁引（其 head-to-head 胜利已被否证）；PrefixShield 2608.01657 占了 "admission responsibility" 术语 |
| D5 | n 的期待管理 | cap-n 不被我们碾压，是帕累托前沿的不同点；立论是"n 不可先验 + 需全量扫描" |
| **D6** | **评测只跑泊松**（09-20）| 齐射/真实到达数据封存，不新增 run |
| **D7** | **Concur 就叫 Concur** | 不加修饰语；两套参数都报（原文 0.2/0.5 与重调 0.35/0.75）；无官方代码，按论文控制律重写，一句带过 |
| **D8** | **不做重复实验**（09-20）| 效率优先，单次结果视作稳定；论文不报误差棒、不声称统计显著性；措辞用"实测为" |
| 新增 | **lab 当独占卡** | 邻居干扰视为意外，不据此设计实验 |
| 新增 | **模型固定** | 就用 Qwen2.5-Coder-7B-Instruct（YaRN×3，96K），暂不扩模型 |
| 新增 | **漂移实验取消** | 池子漂移/负载漂移实验不做；"n 不可迁移"仅用现有三形态数据（齐射/泊松/真实）的对比支撑 |

---

## 5. 本轮（09-19 ~ 09-20）发生了什么

1. **撤回一条错误结论**：草稿 §5.3"越准的预测越保守"建立在有缺陷的 oracle 实现上，已删除并标注 oracle 臂作废（`bba5b7c`）。
2. **文档整顿**：README / REVIEW / runs_registry 全部按对齐池重写；历史文档加"勿引用"横幅；原 `HANDOFF.md` 删除，耐久内容并入 `docs/STATE.md`。
3. **创新点口径统一为 3 个**（此前周报 3 个、草稿 5 条、口头 4 个，容易混乱）。
4. **缺陷 A/B/C 全部结案（重要）**：
   - 根因：驱动重试会复用 run 目录，控制器比 runner 早启动约 4 分钟，**读到了上一次尝试残留的 `admissions.jsonl`**；trace 复用 session id，旧事件把会话标成 done，新一轮的 `queued` 不重置 → 181 个会话"消失" → 队列归零、引擎空转、W 钉在 1 → 死锁。
   - **结论：之前"Concur 论文参数死锁"是我们的 bug，不是 Concur 参数问题。**相关说法已全部撤回。
   - 修复：忽略早于控制器启动的事件 + 补 `admit` 处理 + `queued` 重新入队；回归测试 `tests/test_aimd2_stale_state.py`、诊断脚本 `tests/replay_admission_state.py`。
5. **确认 Concur 无官方代码**（arXiv 2601.22705v1 全文无 repo/artifact 链接）。
6. **队列改造**：从一次性 driver 换成"工作流 + 清单 + 看门狗"自愈结构；快等脚本把 25 分钟等待降到 1 分钟（池子守卫才是真正的保护）。
7. **新增两张图**：泊松状态总览、TTFT CDF。
8. **SLO 阈值敏感性**：1s–60s 排序不变，10s 落在"健康簇（2–3s）"与"崩坏簇（几分钟）"之间的空档。

---

## 6. 中间浮现的 idea（尚未执行，供参考）

- **margin（m=1.15）扫描**：与 ρ 同一权衡轴的另一旋钮。若 ρ=0.90 没有全胜，可试 m=1.05/1.25（2 个 run，约 10 小时）。
- **端到端 SLO**：现在 SLO 不含门外排队；可用 `admissions.jsonl` 的 queued→admit、pause→resume 间隔 + first_token_ms 算出"从到达算起"的口径，作为敏感性列（纯日志分析，不占 GPU）。
- **K3 突发准入**：我们 30s 窗口内准入 ≥2 个的占比 38%（cap2 只有 18%），疑因"增长相关性被忽略"导致同步增长超额 → 可能的机制改进（限速批量准入）。
- **cap1 为什么慢**：并发 1 ⇒ 工具等待无法被其他会话填充，999 步纯串行；它同时是"质量最高、最慢"的极端点，是叙事素材。

---

## 7. 明早结果出来后的决策树

1. **`t90`（ρ=0.90）四项全胜 cap2？**（判据：命中 ≥67%、墙钟 ≤305min、SLO ≥89.5%）
   - **是** → 主表换成 ρ=0.90，论文头条改"四项全胜"；`t85` 可考虑取消。
   - **否** → 看差在哪一维：质量差 → 试 margin 扫描；墙钟差 → 保留 ρ=0.95 并如实写"质量同级、吞吐略优"。
2. **`aimd2_paper`（Concur 原参数）表现如何？**
   - 正常且明显低于 Foyer → 直接写进主表，Concur 一节成立；
   - 仍异常 → 检查是否又是环境问题（先看 `controller.jsonl` 与 `admissions.jsonl` 的时间戳关系，确认无陈旧日志污染）。
3. **`foyer_hc`**：给机制②补上泊松档证据（对照无 HiCache 的 299.3min/65.6%）。
4. 全部落地后：更新 `docs/tables.md`、两张图、草稿 §5.2/§5.8，并把"巡检可以停止了"。

---

## 8. 基础设施速查

- **机器**：lab-3090 = `100.64.0.3`（ssh 别名 `lab-3090`），4×3090；目录 `/data/xbw/turnstile/`。
- **双脚本树**：`scripts/` 与 `TraceLab/replay/scripts/` —— **lane 实际调用后者**，改代码两处都要同步（md5 校验过）。
- **关键脚本**：`run_matrix.py`（起服/池子守卫/memfrac 解算/记录）、`controller_budget3.py`（=Foyer）、`controller_aimd2.py`（=Concur）、`queue_stream.sh`、`watchdog.sh`、`migrate_gpu3.sh`、`status_snapshot.sh`、`make_tables.py`、`gen_registry.py`。
- **每个 run 目录**：`summary.json` / `metadata.json` / `steps.jsonl` / `admissions.jsonl` / `controller.jsonl` / `metrics.csv` / `runner.out`。
- **池子守卫**：目标 `TURNSTILE_EXPECT_POOL`（默认 101432），不符则解算 mem_fraction 重试，最多 0.90，仍不符则中止且不记录。
- **外部击杀是常态**（这台机器有 periodic process-reaper）；看门狗负责拉起，失败都写日志。

---

## 9. 协作约定（用户偏好，务必遵守）

- 聊天消息**不要用 LaTeX**（TUI 渲染不出来），公式用纯文本/缩进。
- 结论表述保持**可核查的证据边界**（已被外部审计抓过三次过度自信）。
- 术语：说 **Concur**（不加"忠实"）；说 **重复实验**（不说"误差棒"）；三个创新点。
- **效率优先**：不擅自加实验、不加重复、不扩模型/场景；要跑新东西先给方案和成本。
- 用户要求**自主推进**，但"凡发车必验启动"；改代码前确认 lane 实际引用的路径。
- 不再使用的数据直接丢弃；文档要标注"当前/历史"，历史文档加横幅。
- 用户会写周报，需要时提供素材（注意周报口径）。

---

## 10. 已知风险 / 未解决

- **主场景仍未全胜**：泊松档 Foyer 65.6% vs cap2 67.7%（墙钟我们快 3.6%）——`t90` 就是为解决它而排的。
- **机制③（有界复活）未实现**，论文只能标"设计"。
- **Concur 复现无官方代码**，保真度只能靠论文描述 + 冒烟测试。
- **`migrate_gpu3.sh` 的时机竞态**：它依赖 cap1 的 summary 出现后 5 分钟内杀掉旧流；若恰好旧流开始跑下一个 lane，可能有 1 个被取消的 run 留下部分目录（无 summary，不影响制表）。
- **论文草稿里仍有过时数字**：摘要与 §1.3 的 69.8%/92.6%/+21.4pp 是旧配置（agg+HiCache）；等配置冻结后统一改。
- **`docs/runs_registry.md`** 由脚本生成，新增 run 后需要重新生成。
