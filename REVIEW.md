# 评审简报（给外部审核 agent / 人类合作者）

> 这份文档是给"想快速审计这个项目"的你看的：我们的主张、证据位置、已知弱点、以及我们**希望你攻击的具体问题**。所有数字可溯源到 `results/night/` 下的原始日志。

## 我们的主张（一句话）

Agentic LLM 负载的 KV 需求在会话到达时就可观测（首条 prompt + trace 中的历史增长轨迹），用它做**前馈式准入控制**，在显存受限的单卡上优于：(a) 不限流，(b) 需要逐档离线调参的静态并发上限，(c) Concur 式缓存反馈 AIMD——且不付吞吐代价。

## 实验设置（详见 paper/draft-v1.zh.md §4）

- 真实 coding-agent trace（UW TraceLab 脱敏版）会话级闭环重放到 SGLang 0.5.10 + Qwen2.5-Coder-7B（YaRN 96K）
- 单卡 RTX 3090 24GB；KV 池 25/50 档 101,432 token、200 档 114,495 token
- 三档负载：25 / 50 / 200 会话同时到达，每会话中位 5 轮、首 prompt 中位 17K、峰值中位 32K

## 核心结果（三档 × 五策略，全部已跑完）

| 负载 | 策略 | wall(min) | 失败 | 命中率% | TTFT p50(s) | SLO% |
|---|---|---|---|---|---|---|
| 25 | cap6 (oracle) | 27 | 0 | 33.9 | 14.8 | 40.0 |
| 25 | **Foyer (v0.5)** | 31 | 0 | **51.5** | **4.3** | **69.6** |
| 50 | cap6 (oracle) | 74 | 0 | 17.7 | 34.8 | 17.2 |
| 50 | **Foyer (v0.5)** | **64** | 0 | **58.1** | **4.3** | **67.6** |
| 200 | cap4 (oracle) | 234 | 1 | 49.6 | 5.6 | 62.3 |
| 200 | **Foyer (v0.5)** | **227** | 1 | **56.9** | **4.0** | **71.9** |

（表中省略 default/aimd/v0.4 行——default 三档全崩、aimd 命中率 10–12%、v0.4 输给 aimd；完整表见 `results/night/comparison.csv` 与论文草稿表 1。）

关键交叉验证：**aimd 对手经过 3 轮调参**（不是稻草人）；**oracle cap 每档用各自最优值**（6/6/4，本身就说明静态上限不可部署）；Foyer 参数三档共用。

## 机制（三组件，各治一种已定位的失败模式）

位置：`scripts/controller_budget2.py`

1. **高水位空闲判据**：投影 floor 用衰减高水位（半衰期 ~5min）而非瞬时 token_usage——撤回风暴后的假跳水不再泄漏准入（治 v0.4 的 cap 4→64 棘轮）
2. **单飞准入**：每周期至多放 1 个，且上一个新会话完成 ≥1 轮才放下一个（治批量 prefill 踩踏缓存）
3. **TTFT-SLO 熔断**：近期 p50 TTFT > 10s 冻结准入（唯一反应式成分，定位为安全阀）

## 我们已知的弱点（不用重复指出，但欢迎深化）

1. **相关工作撞车风险（当前最大风险）**：SGLang 引擎内部有请求级的 `new_token_ratio` KV 预留（`schedule_policy.py`），它在我们所有 baseline 里默认开启——它管不到会话级未来增长与跨会话缓存留存，但"引擎级 vs 会话级"的边界必须在论文里写清楚。Concur（AIMD）已复现为 baseline。**正在做系统性的相关工作排查**。
2. **单次运行、无误差棒**：关键配置 ×3 重复正在跑（lane repA/repB）。
3. **消融未完成**：三组件的消融（`budget2_nohw/nosf/nosv`）在跑。
4. **单模型单卡**：只有 Qwen2.5-Coder-7B + 3090。
5. **无任务质量指标**：trace 脱敏后无任务结果（admission 不改输出语义，影响限于时延/失败——论文声明）。
6. **aimd 调参强度**：3 轮调参后仍输一个量级，但"调到极致的 aimd"的扫参网格还没做。

## 希望你攻击的具体问题

1. 命中率 56.9% vs oracle 49.6%（+7.3pp）在 200 档——这个差距对单次运行来说够稳吗？哪些统计检验或重复设计能让它站住？
2. 我们的 TTFT 是"执行相"（不含门外排队）。会话级完成时间（含准入等待）Foyer 是否反而更差？数据在 steps.jsonl，欢迎直接算。
3. SLO 阀是反应式的——这与"前馈优于反馈"的主张是否自相矛盾？我们的定位是"安全阀"，消融会给出它的贡献占比。这个辩护够吗？
4. 高水位衰减 γ=0.995@2s、target 0.75、margin 1.15——这些常数是否隐式过拟合了这批 trace？换 trace 分布（更长会话/突发到达）预期怎么变？
5. "会话到达时需求可观测"这个前提在真实系统里成立吗？我们的重放里首 prompt 全量可见；如果 prompt 是流式生成或私有上下文呢？
6. 与引擎级 `schedule-conservativeness` 调大的对比（2.0/4.0）已排队——你觉得这足以回应"引擎自己就行"的质疑吗？
7. 论文定位：这个结果你觉得适合投哪里？CCF-B 系统（ICDCS/Middleware/IPDPS 档）还是二区刊（FGCS/JSA 档）？

## 数据与代码导航

- `results/night/<run>/`：每 run 的 `steps.jsonl`（逐轮 TTFT/时间戳）、`admissions.jsonl`（准入事件+等待）、`controller.jsonl`（控制器决策轨迹）、`metrics.csv`（引擎指标 5s 采样）、`summary.json`
- `results/night/comparison.csv`：全 run 聚合
- `scripts/run_matrix.py`：单 run 驱动（起 server → 挂控制器 → 跑重放 → 归档）
- `paper/draft-v1.zh.md`：论文工作稿（含全部数字与叙事）
- 状态：重复实验与消融 2026-09-10 中午前出齐
