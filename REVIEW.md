# 评审简报（2026-09-19 版）

> 给想快速审计这个项目的人：我们的主张、证据位置、已知弱点，以及我们希望你攻击的具体问题。
> 本文件取代 2026-09-09 的旧版简报（那一版描述的是 114,495 token 池、v0.5 控制器，结论已全面作废）。
> 所有数字可溯源：`docs/tables.md` 由 `scripts/make_tables.py` 从 `results/night/<run>/` 的原始产物一键重建；每个 run 的池子、trace 哈希、代码哈希、argv 在 `metadata.json`。

## 一句话主张

Agentic 会话的 KV 需求在**到达时刻就可观测**（首轮 prompt 的实测长度 + 会话内历史增长轨迹）。用它做**前馈式容量核算准入**，可以在显存受限单卡上不调参地守住质量；而静态上限（cap-N）的"好值"只能靠全量扫描得到、且随池子/负载/目标漂移。

## 系统（代码名 → 论文名）

`budget3` = **Foyer**（本文）：每 2s 维护一本显式账目

```
m × [ Σ_{已准入}(x_i + ĝ_i) + (x_新 + ĝ_新) ] ≤ ρ × C_pool
```

`x` = 实测上下文（新到者取首轮 prompt），`ĝ` = 未来 H=3 轮增长估计（会话内 EMA + 全局先验），`m`=1.15，`ρ`=target。控制器写下的不是并发数而是一组**命名许可**（确切会话 ID），runner 按名字执行——"计划放行的"与"实际运行的"由构造保证一致，并支持轮边界 pause/resume。

保留的安全阀：瞬时占用硬闸、TTFT-SLO 熔断（唯一的反应式成分）、饥饿保护。已回收：高水位地板、单飞准入（§5.7）。

## 主张与证据强度

| # | 主张 | 证据 | 强度 |
|---|---|---|---|
| 1 | 无控制会崩溃 | 200 档：命中 0.0%、TTFT p50 40.3min、121/999 步失败 | 已确立 |
| 2 | 静态 cap 甜点区极窄，n 不可先验 | 25 档全扫描：cap3→cap5 命中 64.9%→36.6%，cap7 归零 | 已确立 |
| 3 | 按容量核算可免调参地守住质量 | 见下方主表；**但尚缺"n 会随池子/负载失效"的直接实验** | 部分（核心缺口） |
| 4 | 挂起可以状态保留（HiCache），使暂停可逆 | cap6 31.7%→64.1% 命中（同 cap）；在我们的控制器上 −3.2pp SLO 换 −23% wall | 已确立，代价边界已如实刻画 |
| 5 | 驱逐–代价曲线是 U 形，存在内部最优 | 45 个 run，扣除 981,942 token 的结束清理地板后：压力驱逐 0/4/40/47/79/83（K/会话）对应 wall 63.3/30.4/28.7/**27.9**/32.1/33.4min | 已确立 |
| 6 | **保护层会随能力演进变成天花板** | 回收高水位地板+单飞准入：25 档完成时间 −47%（62.1→33.0min，命中 −3.3pp，n=2） | 已确立 |
| 7 | 预测器（EMA vs 零增长/全局均值）有独立收益 | 25 档四臂质量带 65.7–66.8%，EMA 比零增长慢 17.4% 只多 1 个请求过线 | **未证明**（oracle 臂实现有缺陷，结论已撤回） |
| 8 | 有界复活（W/T 优先级、按层级定价） | 设计与分析 | **未实现、未验证**（论文如实标注） |

## 核心数字（统一 KV 池 101,432 token）

**200 齐射档**（`docs/tables.md` §5.2）

| 策略 | wall(min) | 命中% | SLO% | 失败 |
|---|---|---|---|---|
| 不限流 | 228.1 | 0.0 | 0.2 | 121 |
| cap2（最优静态）| **301.7** | **67.0** | **90.0** | 0 |
| cap3 / cap4 / cap5 | 249.4 / 265.6 / 265.5 | 59.0 / 45.1 / 28.0 | 76.9 / 52.8 / 31.0 | 0 |
| Concur 复现（3090 重调版）| 291.4 | 48.4 | 65.9 | 0 |
| Foyer（旧配置）| 459.1 | 69.8 | 92.6 | 0 |
| Foyer（回收节流后）| 311.4 | 64.8 | 87.3 | 0 |

**到达形状**：泊松 λ=0.04/s（200 会话）Foyer 299.3min/65.6%/88.6% vs cap2 310.5/67.7/89.5；真实高峰小时（117 会话）Foyer **79.3**min vs cap2 131.9min，SLO 并列 100%。

**Concur 复现（进行中）**：论文原参数（u_low=0.2, u_high=0.5）**死锁**（缺陷 C，机理未查清）；单独抬任一阈值可跑通且质量不低于 0.5（ul35 287.5min/45.2%，uh75 283.8min/58.0%）——与论文附录"0.5 附近最优"相反，但每个 arm n=1、且缺 (0.35,0.75) 双因子臂，**不能下结论**。

## 已知弱点（请优先攻击这些）

1. **主场景没有赢**：200 齐射档与 cap2 接近打平（质量 −2.2pp、wall +3%）。我们的叙事是"同质量、免调参"，但**支撑该叙事的漂移实验还没跑**——目前"n 会失效"是从悬崖曲线+Concur 附录推断的，不是直接测量。
2. **误差棒缺失**：关键配置绝大多数 n=1（仅节流消融有 n=2）。
3. **SLO 口径**：TTFT 从"拿到许可、请求提交后"起算，不含门外排队（所有受控臂同口径；排队代价体现在 wall 与会话完成时间）。默认档（无门）的 40min TTFT 是全口径。这个选择是否公平，请攻击。
4. **缺陷 B（未查清）**：控制器决策日志与准入日志的状态对账不上（430 条决策里 waiting_n 峰值 16，日志有 200 个 queued）。未查清前不改控制器。
5. **单模型单卡、重放语料**：tool_wait 取自 trace 分布而非真实工具执行；任务成功质量不可测（脱敏数据无任务结果）。
6. **HiCache 的代价归因**：−23% wall / −3.2pp SLO 是在我们控制器上的测量，机制解释（KV 保活→驻留更久→占预算）尚是推断。

## 已作废、勿引用

- 池子污染的 run（48,234 / 84,528 / 97,367 / 114,495）：已移入 `results/night/_invalid/`；
- 全部旧 aimd2 结论（阈值偏离论文 + H 门失效）；
- oracle 预测臂的解释（实现缺陷：恒定返回生命周期总和、不衰减、不受 3 轮窗口约束）；
- `cap4_r2/r3`（池子 114,495 且 trace 版本不同）；
- 早期 25/50 档（三种池子混用）；
- `REVIEW.md` 旧版与 `REVIEW2.md`、`reviews/`（历史交锋记录，勿当作现状）。

## 我们希望你攻击

1. 容量核算是否只是"把扫参数换成了调 `ρ`/`m`"？我们声称不是（不需要逐负载重扫、且对负载漂移不敏感），但证据还差漂移实验。
2. 命名许可（写确切会话集合）相比"标量上限 + FIFO"是否真的必要？我们给出的必要性论证是非 FIFO 抢占下"计划≠执行"，请检验。
3. U 形驱逐曲线是否稳健（是否受"结束清理地板"扣除方式影响）？
4. 在什么条件下容量核算会输给静态上限？请构造反例要求我们补实验。

## 版本

当前 HEAD 与重建方法见仓库 commit 记录；`docs/tables.md`、`docs/runs_registry.md` 均为自动生成，审计时请以它们与 `results/night/*/metadata.json` 为准。
