【每小时巡检 · Foyer 实验队列】

只读检查，不要改代码、不要启动新实验、不要重启或终止 lab 上的任何进程。

0) 去重
   - 如果距上一次巡检不足 45 分钟（例如重复投递、测试消息），只回一行：【巡检跳过（距上次不足 45 分钟）】，不要重复执行下面的检查。

1) 工作流与当前任务
   - ssh lab-3090：确认两条 queue_stream 是否都存活（gpu2 / gpu3）
   - 读 results/night/status_snapshots.log 的最后一块（lab 上每小时自动写的快照）
   - 找当前在跑的 run（ls -t results/night/*/steps.jsonl | head -2），从 runner.out 末行取进度

2) 异常信号（重点）
   - queue_driver.log 与 watchdog.log 在本小时是否有：重试、POOL MISMATCH、看门狗重启
   - 当前 run 的 steps.jsonl 是否超过 40 分钟没有更新（可能是卡死）

3) 新结果
   - 最近 70 分钟内落地的 summary.json → 读 wall / 命中率 / SLO / TTFT p50，给一行摘要

4) 汇报纪律
   - 一切正常、无新结果、无异常 → 只回一行：【巡检正常 HH:MM】
   - 有新结果 → 三行以内摘要
   - 有异常 → 明确写出问题与建议动作（例如"gpu2 的 run 卡死 40 分钟，建议查 controller.jsonl"）

5) 停止条件
   - 当泊松主表（default / cap1–cap5 / Foyer / Foyer 重复 / Foyer+HiCache / Concur 原参数）
     与预算扫描（rho=0.90 / 0.85）全部落地后，做一次总结并提醒："巡检可以停止了"
