# 断言台账（2026-09-21 建立）

> **为什么要有这张表。** 今天一天之内，论文里的断言被撤回了三次（117K 算术、台阶、批处理
> 曲线），每一次都是**先写进论文、之后才被打掉**——两次是我自己回头查出来的，一次是外部
> 核查（GPT）查出来的。这不是运气问题，是流程问题：**断言在被写进论文时没有留下"它凭什么"
> 和"它到什么程度"的记录**，于是"实测"和"推论"混在一句话里，撤回的时候也撤不干净。
>
> 本表逐条登记论文里的断言，标注**证据来源**与**状态**。规则：
>
> - **只允许四种状态**，不许写"大概"：
>   - ✅ **实测可复现**：有原始产物，脚本可一键重算
>   - ⚠ **推论**：由实测推出，但本身没有直接测量（必须写明推的是什么）
>   - ❓ **待核**：还没验证，或验证不完整
>   - ❌ **已撤回**：曾经写过，现在不成立（保留在表里，注明为什么错）
> - **论文正文引用任何一条时，必须与这里的状态一致。**状态是 ❌ 的不许出现在摘要或结论。
> - 状态是 ⚠ 的，正文里必须写成推论的样子，不许写成实测。

---

## 一、已撤回的（今天撤的，全部保留在此）

| # | 曾主张 | 为什么错 | 撤回依据 |
|---|---|---|---|
| R1 | 三个并发会话需要 117% 池子，所以**必须超订**才能到高并发档 | **那是五个会话的数**。四个是 92,748/101,432 = 91.4%（按 23,187 中位）；用 GPT 的 23.6K 口径是 93.1%。两种口径都装得下 | 直接算术复核。**注意反向也不能推**："四个装得下"只在中位上下文时成立，p90 是 51,064，两个会话就 100%。**能撤回的只是"必然超订"，不能反推"必然装得下"** |
| R2 | 墙钟-并发是**台阶**（不连续） | 逐请求速率随并发不升反降/持平，聚合吞吐近似 并发×速率 → 墙钟应平滑。且缺口 (1.33,1.44) 处**无观测点**，两点之间判不了连续性 | `scripts/decode_rate.py`；且 GPT 指出"两个点总能反推一个幂律指数" |
| R3 | 1→2 并发使速率**翻倍**（+86%），机制是**批处理** | 两处测量错：①引擎计数器约每 10 秒批量更新，按固定 5 秒差分在**分桶归属**上错配；②未排除 prefill/decode 混合 | `batching_curve.py` 已加废弃头注释，图改名为 `fig_batching_SUPERSEDED.png` |
| R4 | **墙钟 ∝ 并发^−1/2** | **这个指数是我编的。** 由所给数据反推是 `r(n) ∝ n^−0.073` → `T ∝ n^−0.93`；用它对 1.04→1.48 预测墙钟比 1.39，实测 1.21——**模型不吻合**。我用 0.5 算出 1.19 再说"吻合实测 1.21"，是拿编的数验证编的数 | GPT 核查 B 条；已复核 |
| R5 | 账本读到 **92%**、引擎实测 **52%**，所以账本**高估一倍** | **量纲错**：92% 是 `committed`（**含 14.8% 的未来增长预留**），52% 是当前占用。**拿预算比现状**。实测 rl90：`measured/red_line = 79.6%`、`committed/red_line = 99.8%`、预留占 committed 14.8% | 读了 `pois200_foyer_rl90/controller.jsonl` 8678 条决策，GPT 核查 D1 条 |
| R6 | 控制器"按**逻辑上下文之和**记账，因此高估 1.8×"，所以要改读物理占用 | **控制器读的就是引擎遥测**：`measured = floor * pool_tokens`，`floor = max(usage, highwater)`，`usage = sglang:token_usage`。那个 `max(resident, ctx_sum)` 是**中间版本 v3 的缺陷，已经修好了**并在冒烟测试 `engine_truth_beats_ctx_sum` 里钉住。**这条描述的是我们自己已修的 bug，不是普适发现** | 读了 `scripts/controller_foyer.py:315-318` |
| R7 | "14 条策略**全部**落在 297–340 与 257–271 两簇" | **是假的**。cap1 = 0.64/533.6；Concur 原参数 = 1.03/364.0；rl99 = 1.21/362.0；default = 1.69/294.8——四条都不在所述区间内 | `docs/tables.md` 泊松主表逐行核 |
| R8 | "忙时吞吐 +11%"，用于 `1.20× = 1.08×忙占比 × 1.11×忙时吞吐` 的分解 | 与 R3 同源（同一个有缺陷的计数器差分）。**"一半少空转、一半批处理"整体作废**，只剩"忙占比"那一半 | 同 R3 |
| R9 | 装载 0.1s vs 重算 **4.3s**（"均为估算，实测留待后续"） | 实测比值 **216×**：搬运 0.005 ms/token、重算 1.08 ms/token → 20K token 是 0.1s vs 21.6s。旧数字把重算说便宜约 5 倍 | 已改为实测值（§3.2） |
| R10 | 216× 是"**不随模型、上下文、负载改变**的硬件常数" | 过度泛化。KV 字节/token 取决于模型与 dtype；prefill 吞吐取决于长度、批大小、干扰；传输含固定开销与队列。**有限区间内未观察到交叉点，不等于交叉点全局不存在** | GPT 核查 D7 条；`docs/three_paths_20260921.md` 该句需删 |

---

## 二、实测可复现的（✅）

| # | 断言 | 证据来源 |
|---|---|---|
| V1 | KV 池 101,432 token，每 run 校验（±20），不符则重启重试、仍不符中止 | `run_matrix.py` 池守卫；`metadata.json.pool_tokens` |
| V2 | 池子会因邻居占卡**静默缩水**，曾伪造出"质量退化 5.9pp" | 守卫抓到三处；坏 run 在 `results/night/_invalid/` |
| V3 | `cap3_r2` **跑的其实是 cap4**：`cap` 字段 200 条全是 3，但 195/200 次准入发生在 `active=3` 时，落地后 4 个在飞 | 直接读 `pois200_cap3_r2/admissions.jsonl` |
| V4 | 唯一一对同条件重复（污染的 cap3_r2 vs 干净的 cap4）差 **0.6%** | 259.1 vs 257.6 分钟 |
| V5 | 主表 14 个 run 总生成 token 离散度 **0.76%**（700,704–706,021） | `scripts/throughput_decomp.py` 内置闸门 |
| V6 | 不限流：命中率 1.6%、TTFT 中位 29.6 分、SLO 3.0%、121/999 失败（flash crowd 档 12.1%） | `pois200_default`；注意两个数来自**不同档** |
| V7 | 各策略墙钟/命中率/SLO/TTFT 的**数值本身** | `docs/tables.md`，由 `make_tables.py` 重建（已验证与仓库逐字节一致） |
| V8 | MORI 用同一套栈：SGLang **v0.5.10** + **HiCache**，且写"preserved on the CPU but must be reloaded to GPU HBM" | 全文逐字引文，三条 |
| V9 | `W/T` 排序与 `(W+T)/T` 排序在 T>0、同一候选集上**等价**（即 HRRN） | 数学 |
| V10 | ThunderAgent 的准入/恢复门控是**聚合水位线加滞回**：`C_total < Σ_{τ=R} c_p + Σ_{τ=A} c_q·f(t_q)`；暂停时 **KV 被释放**（"releases its KV cache for preemption"）；引擎是 **vLLM** | 全文 §4.3 引文 |
| V11 | 池子的 mem_fraction 求解可收敛（修好求解器后 1 次命中 101,432） | `run_matrix.py`；2026-09-21 11:06 实测 |

---

## 三、推论（⚠ —— 正文必须写成推论的样子）

| # | 推论 | 从什么推出来的 | 缺什么 |
|---|---|---|---|
| I1 | 逐请求解码速率**不随并发上升**（36.4 / 34.6 / 33.4 tok/s） | `steps.jsonl` 的每请求 `total_duration − first_token_ms` | **时间边界与引擎口径不一致**：TTFT 从 `send_instant` 起算而 `total_duration` 从更早的 `start` 起算；`first_token_ms` 是第一个**非空文本增量**，不保证恰好一个 token；`complete_timestamp` 在流结束与重新编码**之后**。相对 5% 的效应不可忽略。且纵轴排除了自身 TTFT、横轴却按 `submit→complete` 全窗口平均（**窗口不对齐**）。**结论只能是关联，不是因果** |
| I2 | 会话内配对中位 **0.95–0.97** | 同一会话在并发 1 与 2 下的轮次配对 | 均值不稳定（4 run 0.976 / 6 run 1.017，**跨过 1**），**只能报中位**；四分位 0.81–1.12 跨过 1，单会话噪声大 |
| I3 | 会话级并发高估引擎在跑请求数约 **2×**，比值随并发 0.64→0.38 漂移 | `metrics.csv` 的 `num_running_reqs` vs 会话并发 | **"偏差"这个定性可能是错的**：会话数本来就不等于运行请求数，除非拿它去估计后者，否则是定义差异不是偏差（GPT D1） |
| I4 | 在飞会话数高估与逻辑上下文高估**不独立** | —— | GPT 指出：tool_wait **同时**减少锁定 KV 和运行请求，所以两个"独立来源"不独立 |
| I5 | 高水位地板虚占 **29%** 准入预算、回收后完成时间 **−47%**（命中率 −3.3pp） | §5.7 消融；25 会话档 | 需确认该数是哪个档、哪次运行，并绑定配置版本 |
| I6 | 搬运比重算便宜 **216×**，因此"搬还是算"是容量约束不是代价权衡 | 0.005 vs 1.08 ms/token | **测量定义要交代**：重算单位成本是隔离 prefill 还是含排队的 TTFT？搬运是完整恢复路径还是异步提交？受拥塞的 TTFT 回归斜率与空载 DMA 微基准不能当同一口径 |
| I7 | 4 个并发会话装得下（91.4%） | 中位上下文 × 4 / 池子 | 只在会话都停在中位上下文时成立；平均并发 3.94 **不证明**四份完整上下文同时驻留且无驱逐 |

---

## 四、待核（❓ —— 不许进结论）

| # | 断言 | 缺什么 |
|---|---|---|
| Q1 | "逻辑上下文之和比物理驻留 KV 高估 ~1.8×" | **从未在同一时刻、同一会话集合上量过。归因未成立。两条候选解释都已被源码否掉**，见下方 Q1 专节 |

### Q1 专节：两个候选解释都被否掉了（2026-09-21 晚，读 lab 上安装的 SGLang 源码）

**候选一（GPT 提出）：`token_usage = (pool − available − evictable)/pool`，即扣除了可回收缓存。**
那个式子确实存在，但在 `srt/managers/scheduler_runtime_checker_mixin.py` 的
`_get_token_info()` 里——**一个供内部检查用的辅助函数，不是 Prometheus 指标的来源**。
指标的来源是 `srt/observability/scheduler_metrics_mixin.py`，另有定义。**该引用张冠李戴。**

**候选二（我提出）：`full_token_usage − token_usage` 就是可回收部分。**
看起来很有说服力：差值恒 ≥0；随并发**反向**变化（cap1 有 24.8% 的采样不等、cap4 只有 0.10%）；
幅度中位 21.6pp、最大 74.2pp；而且**持续存在**——cap1 里最长连续 54 拍 = 270 秒，
**不是抓取竞态**（竞态只会产生单拍毛刺）。行为完全像可回收缓存。

但两个 gauge 自己的说明文字是：

```
sglang:token_usage       "The token usage."
sglang:full_token_usage  "The token usage for full attention layers."
```

**`full_token_usage` 是混合注意力模型里"全注意力层"的用量，不是"含可回收的全部驻留 KV"。**
而且 `is_hybrid_swa` 是**按架构白名单**判定的（Llama4 / GptOss / MiMoV2 / Step3p5），
`Qwen2ForCausalLM` 不在其中；源码里该模型走的那条路径下两者被赋成**同一个值**。
**源码与实际观测矛盾，机制未查明。**

**结论：这两列给不出"锁定 vs 可回收"的分解，Q1 必须实测，不能从盘上已有的列反推。**
**值得记的是：候选二当时看起来同样自洽、同样"有数据支持"——
它没变成今天第四条被撤回的断言，唯一原因是先查了源码。**
| Q2 | 驱逐地板 ~982K token/run 与策略无关 | 需确认数据来源与"无关"的验证范围 |
| Q3 | 高水流地板的代价、HiCache 的代价边界等 §5.7/§5.4 数字 | 需绑定配置版本（控制器改过多次） |
| Q4 | Concur 复现的墙钟（364.0 / 264.6） | **两条 arm 的 `wall_s` 都是 None**（被 process-reaper 杀掉、metadata 未收尾），现用步骤跨度估计。需注明 |
| Q5 | "配对实验的配对差" | 尚未跑。且 GPT 指出：两臂共享 CPU/内存/PCIe/主机 cache 带宽，**可能互相干扰**；一轮换卡不足以估计方差，需多个随机化交叉 block + 少量独跑对照 |
| Q6 | 浮点：`ΔΣcounter / ΣΔt = (末值−初值)/总时间` 是否成立 | 成立，但**分桶归属**是另一回事。`33.5 → 45.1` 是同一桶还是全局、是否含零样本、边界/权重是否一致，**未交代** |

---

## 五、代码级的已知问题（GPT 指出，已核）

| # | 问题 | 状态 |
|---|---|---|
| C1 | `throughput_decomp.py` 用 `max(gen)` 处理计数器重启 —— **`max` 不是多段重启后的总量**，应按单调 epoch 分段求和 | **✅ 已修**（改为按单调 epoch 分段求和）。本批 run 无重启，**数字未变** |
| C2 | 同脚本逐字段 `append` 在单字段缺失时**数组错位**，之后 `zip` 静默错配 | **✅ 已修**（改为整行解析成元组，全字段成功才追加）。**我今天自己在临时脚本里踩过同一个坑并修好了，却把这个 bug 留在了正式脚本里** |
| C3 | **已核并量化，影响可忽略。** 代码确实混用起点：`total_duration_ms` 从构造 payload **之前**起算、`first_token_ms` 从**之后**起算（两者还都是 `SystemTime` 而非 `Instant`）。**但实测**：payload 构造时间中位 **0.389 ms**、最大 5.61 ms，而解码窗口中位 **11,214 ms** —— 占比 **0.0035%**。代码里"no client-side decode"那句注释属实 | **✅ 已核，不影响结论**。分析已改为用同源的绝对时间戳（`post_timestamp → complete_timestamp`）计算窗口，两种口径给出**完全相同**的 36.4/34.6/33.4 |
| C4 | `pausing_tokens` 直接加在 `measured` 上 → **重复计入** | **✅ 已证实并修复**。证明不是推测：rl90fix 日志里 `measured 97,180 + pausing 15,700 = 112,880 > 池子 101,432`——**引擎不可能持有超过池子的不可回收 KV**，所以 `measured` 已含该会话。后果非边际：`committed` 120–132k 对 91,289 红线，**大部分周期一个都放不进**。已改为只记日志、不计入 committed；新增测试钉住不变量。**那个 run 已停，队列用新代码重启** |
| C5 | 一次上五个修复，**无法归因**是哪一个起作用 | 已确认。**这是当前最大的设计缺陷**：rl90fix 重启后与 rl90 之间差了五个改动（4 个记账修复 + pausing），差值无法拆到单项。GPT 建议做单项消融——**需要额外卡时**，见下方行动项 |

---

## 六、行动项状态（2026-09-21 晚）

| # | 行动 | 状态 |
|---|---|---|
| 1 | 修 C1/C2（产出论文数字的脚本） | ✅ 已修并部署。**数字未变**（这批 run 无计数器重启），属正确性保险 |
| 2 | 核 C4（计账重复计入） | ✅ 已证实、已修、已停掉受影响的 run、已加测试 |
| 3 | 核 C3（时间边界） | ✅ 已核并量化为 0.0035%，分析已改为同源窗口 |
| 4 | Q1 新测量：同一时刻同一集合上分别量五类资源 | ⏳ **未做**。控制器已在记大部分，加日志即可。**这是框架能否立住的关键测量** |
| 5 | Q5 配对实验重新设计 | ✅ 协议已写：`docs/pair_design_20260921.md`。**暂不追加波次**——框架未定，先确定要不要这个数再花卡时 |
| 6 | 每个实验行绑定控制器版本 hash | ✅ **机制已在工作**：`code_version()` 记 `controller_foyer.py` / `session_runner` / `path_divergence`，实测 t90 与 cap4_hc 的 metadata 都有。**但表还没绑定**——见下方"版本事实" |
| 7 | C5 单项消融（拆开五个修复） | ⏳ **未做**。当前 rl90fix 与 rl90 差五个改动，无法归因。需要额外卡时 |

---

## 六之二、版本事实（做全表绑定前必读）

```
t90     controller_foyer.py = 56472845dd83
cap4_hc controller_foyer.py = 56472845dd83   （正在跑，同 hash）
lab 当前 controller_foyer.py = c213219d6007   ← pausing 修复后
```

- **不同 `controller_foyer.py` hash 的行不能直接并列进同一张表。**
- **但要先看那一行是否真的用了该控制器**：
  - `static=N` 行**不执行控制器**（走 cap-file），hash 不同无妨；
  - `budget3:...` 行用的是 `controller_budget3.py`，**不受 `controller_foyer.py` 改动影响**；
  - `foyer:...` 行才用 `controller_foyer.py`。
- **具体影响**：今晚的**配对实验两臂**是 `static=2` 与 `budget3:hw=0;target=0.95`
  → **都不受我今天的 `controller_foyer.py` 修复影响**，可以放心跑。
  而 **rl90fix 是 `foyer:` 路线**，重启后与 rl90 之间**差了五个改动**（见 C5）。

---

## 七、这张表要维护，不是一次性

**每次往论文里写新断言，就在本表加一行；每次撤回，把行移到第一节并写明为什么错。**
今天三次撤回之所以代价大，就是因为它们写进去的时候没有这张表。

---

## 八、Q1 追加：机制查明（2026-09-21 深夜，直接采样 metrics 端点）

**先撤回我 20 分钟前的警报**：我曾看到端口 30033 上 `token_usage=0.0` 而
`full_token_usage=0.834`，判断"控制器是瞎的、`measured` 不可信"。**那是误读，收回。**

连采四拍，机制很清楚：

```
running=0  ->  token=0.000   full=0.834     ← 空闲
running=1  ->  token=0.194   full=0.194     ← 有请求，两者相等
running=1  ->  token=0.205   full=0.205
running=1  ->  token=0.224   full=0.224
```

**有请求在跑时两者恒等；空闲时 `token_usage` 正确归零，而 `full_token_usage` 停在上一次的值。**

这一条**完整解释了 §四 Q1 那个"完整 attention 层"的谜**（此前只能说是"源码与观测矛盾"）：

| 观测到的现象 | 由"空闲时 full 不变"解释 |
|---|---|
| 差值只出现在部分采样 | 只出现在空闲采样 |
| 与并发**反向**（cap1 24.8% / cap4 0.10%） | 低并发空闲时间长 |
| 幅度最大到池子 74pp | 陈旧峰值 − 当前值，空闲越久越大 |
| 持续到 270 秒 | 空闲时段本来就那么长 |
| 恒 ≥0 | 陈旧值 ≥ 当前值 |

**结论（两条，都是可用的）**
1. **`token_usage` 是可信的**——它跟着现实走，空闲归零。**控制器的 `measured` 没有语义问题。**
2. **`full_token_usage` 是陷阱**——空闲不更新。**任何分析都不许拿它作参照或分母。**
   写进 `monitor.py` 的抓取说明里，免得以后有人再用它。

**顺带（对论文有用）**：cap4_hc 上 `sglang:hicache_host_used_tokens = 201,420`，
`hicache_host_total_tokens = 202,865`，而 GPU 池只有 **101,432**——
**主机层持有约两倍于 GPU 池的 token，且已用 99.3%。**
主机层不是"小缓冲区"，它是同量级的存储。之前论文里对 HiCache 的表述偏保守了。

**未查明**：源码里非 hybrid 路径下两者应被赋成同一个值，与观测不符；
具体是哪条路径导致 `full` 不更新，没有追到底。**但结论不依赖它**——
上面两条结论是直接从端点的实测行为得出的。

---

## 九、Q1 结案：1.8× 是拿预测比实测，而且对应的控制器已经不存在了

**来源定位**：`docs/throughput_decomposition_20260921.md` §4 末尾

```
这与主表"账本读到 92%、引擎实测 52%"是同一件事的两个侧面：
逻辑上下文之和 → 物理驻留 KV      高估 ~1.8×   （前缀共享）
```

**92 / 52 = 1.77。** 这就是 1.8× 的全部来历。

**为什么它不成立**（两个独立的理由，任一都足够）：

1. **量纲不对等。** 92% 不是"逻辑上下文之和"，是 `controller_budget3.py` 的
   `projection / budget`，而 `projection = max(engine_truth, sharing_blind_logical_sum)
   + growth reserve for EVERY resident`——**一个含未来增长预留的预测上界**。
   52% 是引擎当前实测。**预测上界高于当前值，是预测该有的样子，不是估计偏差。**
   （GPT 在 D1 条已经点过这一处；今天找到了它的出处。）
2. **对应的实现已经不在树上了。** 那个三重悲观式的 v3 控制器已被
   `controller_foyer.py` 取代，后者明确不再建模逻辑和、只读引擎遥测。
   所以 1.8× 描述的是一个**已废弃实现**的行为，不能作为现行方法的证据。

### 同量纲直接实测（2026-09-21，99 拍，rl90fix 运行中）

用 `controller_foyer.py` 新增的第 8 步日志（`ctx_sum` vs `measured`），
影子控制器读真实 run 的 `admissions.jsonl` / `steps.jsonl`（只读；它写的 permit
文件无人消费，决策是惰性的），所以 `ctx_sum` 跟的是真实会话集合：

| resident | running | n | ctx_sum/measured 中位 |
|---|---|---|---|
| 1 | 1 | 4 | 0.994 |
| 2 | 1 | 34 | 1.158 |
| 4 | 1 | 8 | **1.437** |
| 4 | 2 | 18 | 1.067 |
| 4 | 3 | 13 | 0.979 |
| 4 | 4 | 3 | 0.959 |

**结论（可写进论文的）**：同量纲下比值**不是常数**，99 拍里 0.72–1.44 都有，
**符号会翻转**（既有高估也有低估）；单会话驻留时两者几乎完全相等（0.994）。
**"会话侧记账系统性高估引擎占用"这个方向性说法，同量纲下不成立。**
成立的是一个更弱但更有用的说法：**两者之间没有稳定的换算系数**，
按固定系数折算的控制器在两个方向上都可能错。

**不许写**：不许把上表写成机制（"池子装不下所以驱逐"只是**假设**，
n 只有 3–34，且是单配置 200 秒窗口）。要下机制必须换配置重复。
