# 主张 → 证据 审计（2026-09-23）

> 目的：写正文之前，把活下来的每一条主张拉出来逐条查。
> 判据是四样，**缺任何一样就降级或删掉**：
>
> 1. **实测数字**（不是推论）
> 2. **对照**（不是孤立观测）
> 3. **反例检查**（我们自己试过推翻它没有）
> 4. **可复现路径**（命令 / 脚本 / run 名）
>
> 这个项目的三条撤回（`tool_wait` 0.1s→16.85s、oracle 单调性、重复计费）都是因为缺了第 3 条。

---

## C1. 墙钟可以拆成「有效工作 + 空转」两部分

**数字**：`cap4_hc 202.8 + 16.1 = 213.0`；`foyer_hc 250.1 + 47.4 = 291.3`；`cap1 340.0 + 193.6 = 527.4`
**对照**：11 条静态 + 9 条 Foyer，全表一致
**反例检查**：`bres` 是反例形态的反例——它空转压到 10.1% 但有效工作涨到 271 分钟，**证明两项独立**
**路径**：`scripts/frontier.py`；空转从 `metrics.csv` 的 `sglang:num_running_reqs == 0` 段累加

**状态：可用。** 注意措辞——这是**分解**，不是"模型"。

---

## C2. 步时间可以被两个独立方法测到，且两者排序一致

**数字**（反推 / 逐请求）：

```
cap4_hc   29.0 / 25.7 ms        cap5   35.9 / 31.2 ms   ← 两个方法都判异常
cap3_hc   27.3 / 24.5 ms        bres   34.2 / 31.3 ms   ← 都判第二异常
cap2      26.9 / 22.8 ms        hz1    28.5 / 24.6 ms
foyer_hc  28.2 / 23.8 ms
```

**方法一** = 墙钟 × 在跑均值 ÷ 产出 token（从 `metrics.csv` + `steps.jsonl`）
**方法二** = 逐请求 `(total_duration_ms − first_token_ms) / (output_len_actual − 1)`

**系统性差 8–16%，排序完全一致。**
**反例检查**：这正是审计要查的——如果两者不相关，C2 就该删。它们相关。
**⚠ 未解决**：`metrics.csv` 里 `itl_p50/p90`、`e2e_p50`、`ttft_p50` **全是空的**，
引擎侧的延迟指标从未被采集。**方法二是替代品，不是引擎直采。**

**状态：可用，但必须写成"两个导出量互相印证"，不能写成"引擎实测步时间"。**

---

## C3. 引擎空转是结构性的，占墙钟 7.4%–36.3%

**数字**：`cap4 7.4% / cap3 9.9% / foyer 15.9% / bres 10.1% / cap2 18.4% / cap1 36.3%`
段长：中位 5–10 s，p90 20–25 s，最长 366–386 s
**成因**：会话在 `tool_wait` 期间不发请求 + 存活会话只有 ~2 个
**对照**：并发越低空转越多（cap1 36.3% → cap4 7.4%），单调
**反例检查**：`bres` 把空转压到 10.1% 却没变快 → **证明"填空转"本身不产生收益**，这条主张必须带上这个反例
**路径**：`metrics.csv` 的 `num_running_reqs == 0` 连续段累加

**状态：可用。必须同时报 `bres` 的反例，否则会被误读成"填上就能快"。**

---

## C4. 九条 Foyer 变体，零条越过静态前沿

**数字**：

```
hz1     255.8 / 84.8  +0.20  ON（在线上）
foyer_hc 291.3 / 89.8  −1.14  BELOW
hz0     290.0 / 77.2 −13.5  BELOW
bres    295.5 / 83.8  −7.82  BELOW
hz2     306.1 / 86.9  −4.85  BELOW
breshz1 310.9 / 72.3 −19.55  BELOW
nogate  272.3 / 66.6 −20.9   BELOW
rg4     282.3 / 64.3 −24.0   BELOW  (998 步，非跑满)
foyfix  415.0 / 46.1 −48.7   BELOW（全表最差）
```

**对照**：静态前沿锚点 `cap4_hc (213.0,63.4) → cap3_hc (236.4,81.1) → cap2_hc (295.0,91.6) → cap1 (527.4,94.8)`
**反例检查**：`frontier.py` 的判决是机械的，且**它自己抓出过一个 bug**（cap1 被外推判成"越过前沿"，已修）
**路径**：`scripts/frontier.py --all-foyer`

**状态：可用，是本文最硬的一条。**

---

## C5. 四条机制上不同的政策路线，逐一否证且各有原因

```
改记账底座    charge=delta（§四十八，源码） + base=resident（§五十六，端到端）
改增长预留    horizon 0/1/2/3 全扫（§五十七）
改硬上限      target 各种组合
改执行粒度    round-gate 限在跑数（§五十八，机制上不可实现）
```

**每条都成功地抬高了在跑数**（bres 引擎占用 41%→52%；hz1 并发 2.06→2.60），**但都不产生端到端收益。**
**反例检查**：这正是"反例检查"本身——每条都是对前一条的否证尝试。
**路径**：各 run 的 `controller.jsonl` + `metadata.json`

**状态：可用。这是"边界"叙事的核心。**

---

## C6. 节流开关在负载两端方向相反

**数字**：

```
25 档    节流全关 → 58.4 → 28.8 分钟（快 51%），SLO 95.2 → 92.8（−2.4pp）
200 档   节流全关 → 291.3 → 415.0 分钟（慢 42%），SLO 89.8 → 46.1（−43.7pp）
```

**⚠ 弱点**：25 档那批是**旧 harness**，与 200 档不同代，**不能直接混比**。
**补齐动作**：新 harness 下重跑 25 档 —— `cap2_25`、`cap3_25` **已落地**；
`cap4_25`、`foyer_push_25` **被 cjy 占卡打断，未完成**。

**状态：数字可用，但必须标注"跨代"或补齐。补齐只差两条。**

---

## C7. 25 档：静态 cap 仍然打赢 Foyer（新 harness）

**数字**：

```
cap3_25  24.2 分钟 / 并发 2.77 / SLO 91.2 / 段SD 5.1     ← 新 harness
cap2_25  32.4 分钟 / 并发 1.95 / SLO 92.8 / 段SD 11.0
（旧 harness 参考，不可直比）thr25_push 28.8 / 2.28 / 92.8
```

**cap3_25 比 Foyer 最激进档（旧 harness 的 28.8）还快 16%。**
**反例检查**：这条否掉了我自己预注册的读法 ①（我写的是"Foyer 默认配置输"，
实际是"Foyer 最好的一档也输"）。
**⚠ 弱点**：Foyer 那一格是旧 harness，**同代对照还差 `foyer_push_25`（被 cjy 打断）**。

**状态：方向明确，但**同代对照未完成**。这是补齐优先级最高的一条。**

---

## C8. 会话在轮次之间平均停放 16.85 秒

**数字**：799 次轮次转换，实测间隔均值 16.85 s、p90 29.33 s；trace 要求 15.82 / 27.72；比值中位 1.001
**对照**：重放管线忠实兑现（比值 1.001），且与 TraceLab 自报的工具均值 16.8 s 吻合
**反例检查**：**这条本身就是一次更正**——我此前引用中位 0.35 s 得出过反向结论

**状态：可用。而且它是 C3 的成因。**

---

## C9. 缓存命中率是负载自带的，不是政策变量

**数字**：四个不同政策，round1+ 实际命中率 88.8–89.8%，全部等于 trace 的 `planned_prefix_hit_rate` 89.8%
**对照**：`foyer_hc` / `cap3_hc` / `foyer_t85` / `cap2_hc` 四种政策
**⚠ 弱点**：**只测过准入政策，没测过驱逐政策**（SGLang 的 `radix-eviction-policy` 在 v0.5.10 有 `lru/lfu/slru`）。
如果 `slru` 能超过计划值，这条就破。**未做的对照必须写明。**

**状态：可用但带明确边界。**

---

## C10. 上下文异质性

**数字**：prompt_len 中位 23,187；p90 51,064；p99 78,006；最大 89,436（= 池的 88.2%）
**对照**：两个最大会话相加 = 池的 176%，装不下 → **这是"人头制为什么粗糙"的量化依据**
**反例检查**：无（纯统计量）

**状态：可用。**

---

## C11. 逐请求解码速率随并发不升反降

**数字**：并发 1/2/3 → 36.4 / 34.6 / 33.4 tok/s；六个 run 里五个单独看方向一致
**⚠ 弱点**：**这条来自被撤回的贡献 1**——但撤回的是**解释**（"批处理"那条），
**测量本身在修正了采样 bug 之后是有效的**。用的时候只能说测量，不能带旧的因果解释。

**状态：测量可用，解释不可用。**

---

## 汇总：写之前必须补的

| 优先级 | 缺什么 | 成本 |
|---|---|---|
| **高** | `foyer_push_25` 同代对照（C7 的关键一格） | 30 分钟 |
| **高** | `cap4_25`（补完 25 档前沿） | 30 分钟 |
| 中 | 驱逐政策对照（`slru` vs `lru`），C9 的边界 | 4 小时 |
| 中 | 换配置复现（A100 或更大模型）——**分水岭，见下** | 1–2 天 |
| 低 | 补采引擎侧 ITL（`metrics.csv` 那几列为什么是空的） | 待查 |

**阻塞项**：卡被 `cjy` 占用，全部实验暂停。**上面两条"高"加起来只要 1 小时——卡一空就能补。**
